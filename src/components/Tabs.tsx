import React from "react";

const Tabs = (props: any) => {
  return (
    <div className="lg:block hidden">
      <button>
        <div>
          <h5>Haikei for UI Design</h5>
        </div>
      </button>
    </div>
  );
};

export default Tabs;
